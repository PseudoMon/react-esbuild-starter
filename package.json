{
	"type": "module",
	"scripts": {
		"build": "esbuild src/main.jsx --minify --bundle --outfile=dist/bundle.js",
		"dev": "esbuild src/main.jsx --sourcemap --bundle --outfile=dist/bundle.js --servedir=dist"
	},
	"dependencies": {
		"react": "^17.0.2",
		"react-dom": "^17.0.2"
	},
	"devDependencies": {
		"esbuild": "^0.14.27"
	}
}
